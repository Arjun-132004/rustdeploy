apiVersion: v1
kind: Service
metadata:
  name: rustdeploy-backend
  namespace: storefront-devrust
  labels:
    app.kubernetes.io/name: rustdeploy-backend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rustdeploy-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
