apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rustdeploy-frontend
  namespace: storefront-rust
  labels:
    app.kubernetes.io/name: rustdeploy-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rustdeploy-frontend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
    scaleUp:
      stabilizationWindowSeconds: 60
